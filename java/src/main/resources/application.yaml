spring:
  application:
    name: java

  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://base-datos:5432/gastos_db}
    username: ${SPRING_DATASOURCE_USERNAME:usuario_gastos}
    password: ${SPRING_DATASOURCE_PASSWORD:clave_secreta}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: ${JPA_SHOW_SQL:false}

  #Servidor SMTP para los correos de verificación.
  #En desarrollo apunta a Mailpit (docker-compose); en producción se cambia por variables de entorno.
  mail:
    host: ${SPRING_MAIL_HOST:localhost}
    port: ${SPRING_MAIL_PORT:1025}
    username: ${SPRING_MAIL_USERNAME:}
    password: ${SPRING_MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: ${SPRING_MAIL_AUTH:false}
          starttls:
            enable: ${SPRING_MAIL_STARTTLS:false}

#Chat FinBot (Groq). La clave se pasa por variable de entorno, nunca se sube al repositorio.
groq:
  api-key: ${GROQ_API_KEY:}
  model: ${GROQ_MODEL:llama-3.3-70b-versatile}

cors:
  origins: ${CORS_ORIGINS:http://localhost:4200,http://localhost:4300}

#Límite de peticiones por IP para el chat, para proteger la cuota de Groq frente al abuso.
limite:
  chat:
    peticiones: ${LIMITE_CHAT_PETICIONES:15}
    ventana-segundos: ${LIMITE_CHAT_VENTANA_SEGUNDOS:60}